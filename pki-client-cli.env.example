# pki-cert-manager.sh Environment Configuration
# Copy this file and customize for your certificate

# =============================================================================
# Logging
# =============================================================================
# Path to log file (default: /var/log/pki-cert-manager.log)
LOG_FILE="/var/log/pki-cert-manager.log"

# =============================================================================
# PKI Server Connection
# =============================================================================
# PKI server address (IP or hostname)
PKI_HOST="192.168.1.40"

# PKI API port (default: 8889)
PKI_PORT="8889"

# Which intermediate CA to use: 1 or 2 (default: 1)
CA_NUM="1"

# =============================================================================
# Authentication Files
# =============================================================================
# Path to CA bundle file (downloaded from PKI server)
CA_BUNDLE_PATH="/etc/pki/ca-bundle.crt"

# Path to authentication key file (downloaded from PKI server)
# Remember to use intermediate-2-auth-key.txt for CA_NUM=2
AUTH_KEY_PATH="/etc/pki/intermediate-1-auth-key.txt"

# =============================================================================
# Certificate Subject
# =============================================================================
# Common Name (CN) - required
CERT_CN="myserver.example.com"

# Additional hostnames/IPs (comma-separated, optional)
CERT_HOSTS="www.example.com,192.168.1.100"

# Organization (optional)
CERT_O="My Company"

# Organizational Unit (optional)
CERT_OU="IT Department"

# Country - 2 letter code (optional)
CERT_C="DK"

# State (optional)
CERT_ST="Capital Region"

# Locality/City (optional)
CERT_L="Copenhagen"

# =============================================================================
# Key Configuration
# =============================================================================
# Key algorithm: rsa or ecdsa (default: rsa)
KEY_ALGO="rsa"

# Key size:
#   For RSA: 2048, 4096 (default: 2048)
#   For ECDSA: 256, 384, 521 (default: 256)
KEY_SIZE="2048"

# =============================================================================
# Output Configuration
# =============================================================================
# Output filename prefix (default: CERT_CN value)
OUTPUT_PREFIX="myserver.example.com"

# Directory for certificate files
CERT_DIR="/etc/ssl/certs/myserver.example.com"
